% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{import_deconvolution_results}
\alias{import_deconvolution_results}
\title{Import External Deconvolution Results}
\usage{
import_deconvolution_results(spe, results, result_name)
}
\arguments{
\item{spe}{A \code{SpatialExperiment} with unique, non-empty spot IDs.}

\item{results}{A numeric matrix or data frame with spots as rows and cell types
as columns. Row names must match all \code{colnames(spe)} exactly, in any order.
Values must be finite and non-negative; they need not sum to one.}

\item{result_name}{A prefix identifying the external result, for example
\code{"omnideconv_bayesprism"}. Use a syntactically valid R name starting with a
letter. The names \code{"expression"} and \code{"cluster"} are reserved.}
}
\value{
The updated \code{SpatialExperiment}. Results are stored in \code{colData} as
\verb{<result_name>_<cell_type>}, with cell-type names converted using \code{make.names()}.
Ambiguous names and collisions with existing columns are rejected.
Use \code{result_name} with \code{available_results()}, \code{plot_spatial()},
and \code{cluster()} to select the imported result group.
}
\description{
Adds externally computed cell-type estimates to an existing SpatialExperiment.
Rows are aligned by spot ID. Values are preserved without normalization;
expression data, spatial coordinates, and images are unchanged.
}
